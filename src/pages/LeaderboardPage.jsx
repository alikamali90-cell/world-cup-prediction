
Sat, Jun 27 at 7:00 PM

Rules (1).pdf
PDF
ba in ghanonaha mikhaim ye jadvale pishbini dorost koni vase doare aval ghanonar o ziresh benevis in bazia  japan brazil , germany paraguay , netherland morroco , ivory coast norway , france sweden 

Thought for a few seconds
آره، اینم یک فرم پیش‌بینی مرحله Round of 32 بر اساس قوانین فایلت؛ مناسب برای کپی داخل گروه یا تبدیل به PDF. قوانین هم خلاصه زیرش آمده. 


🏆 فرم پیش‌بینی Round of 32 جام جهانی
نام شرکت‌کننده: …………………
تاریخ ثبت فرم: …………………

#	بازی	پیش‌بینی نتیجه	تیم صعودکننده	روش برد
1	Japan 🇯🇵 vs Brazil 🇧🇷	… - …	…………………	90 دقیقه / وقت اضافه / پنالتی
2	Germany 🇩🇪 vs Paraguay 🇵🇾	… - …	…………………	90 دقیقه / وقت اضافه / پنالتی
3	Netherlands 🇳🇱 vs Morocco 🇲🇦	… - …	…………………	90 دقیقه / وقت اضافه / پنالتی
4	Ivory Coast 🇨🇮 vs Norway 🇳🇴	… - …	…………………	90 دقیقه / وقت اضافه / پنالتی
5	France 🇫🇷 vs Sweden 🇸🇪	… - …	…………………	90 دقیقه / وقت اضافه / پنالتی
📌 قوانین امتیازدهی Round of 32
مورد پیش‌بینی	امتیاز
انتخاب تیم صعودکننده درست	2 امتیاز
نتیجه دقیق درست	+2 امتیاز
روش برد درست؛ 90 دقیقه / وقت اضافه / پنالتی	+1 امتیاز
مثال امتیاز
اگر کسی پیش‌بینی کند Brazil 2-1 Japan و برزیل واقعاً صعود کند:

حالت واقعی	امتیاز
Brazil صعود کند، ولی نتیجه فرق کند	2
Brazil 2-1 ببرد	4
Brazil 2-1 ببرد و روش برد هم درست باشد	5
⚠️ قوانین مهم
در بازی‌های حذفی، امتیاز «برنده درست» بر اساس تیم صعودکننده حساب می‌شود، حتی اگر بازی در وقت اضافه یا پنالتی تمام شود.

نتیجه دقیق بر اساس نتیجه پایان وقت اضافه حساب می‌شود، نه ضربات پنالتی.

بعد از شروع هر بازی، پیش‌بینی همان بازی دیگر قابل تغییر نیست.

روش برد یکی از این 3 حالت است:
90 دقیقه / وقت اضافه / پنالتی.


Thought for 1m 4s
آماده‌ست. فایل PDF جدول خالی پیش‌بینی Round of 32 رو از اینجا دانلود کن:

round32_empty_prediction_form.pdf

قوانین امتیازدهی داخل PDF بر اساس فایل قوانینیه که فرستادی. 



Thought for 1m 4s
انجام شد:

«تعداد گل آقای گل» حذف شد

پرچم مراکش جلو اسمش آمد

PDF آپدیت شد

round32_empty_prediction_form_updated.pdf


Thought for 41s
انجام شد، بازی South Africa vs Canada اول جدول اضافه شد.

round32_empty_prediction_form_south_africa_canada.pdf


Thought for 43s
درست شد. پرچم مراکش جلوی اسم Morocco و روی همان خط قرار گرفت.

round32_empty_prediction_form_morocco_fixed.pdf


Thought for 1m 47s
انجام شد، نتیجه الآن داخل ستون بازی و جلوی اسم تیم‌ها قرار گرفت.

round32_empty_prediction_form_score_inline.pdf


Thought for 2m 10s
آماده‌ست، نسخه انگلیسی PDF:

round32_empty_prediction_form_english.pdf


Thought for 1m 15s
آماده‌ست — نسخه کامل انگلیسی با هر 16 بازی Round of 32 اضافه شد.

round32_full_prediction_form_english.pdf

اگر خواستی، نسخه فارسی کامل همین فرم را هم برات درست می‌کنم.


Tue, Jun 30 at 11:50 AM
khob mikham vase doare 32 ke ghanona sho midoni correct score o vared konam point o baram hesab kon va mikham entekhab konam 90 120 ya penalty 


alan mikham data to radife 3 vared konam va az radife 8 be ebad tebghe on moghayese kone va emtiaze bede dorostesh kon 




alan emtiaz dare eshtebah hesab mikone 

nemishe bazam mizane 3 point vali point bayad 0 bashe 

asan chekar row 2 dare mohem nabayd bashe correct score fght age adada yeki bod dg mishe 

Thought for 54s
آماده‌ست، فرم PDF پیش‌بینی Round of 16 با 5 بازی و قوانین زیرش:

round16_prediction_form.pdf


Fri, Jul 3 at 1:35 PM
khob bazihaye jadid o add kardam to excel hala ,ikha, formul ezafe konam havaset bashe tebghe emtiaze dehi jadid round 16 bashe va inke total points be column CD ezafe she 

 ina ham hast

Thought for 52s
فرم Round of 16 آپدیت شد و بازی‌های Argentina vs Egypt و Switzerland vs Colombia هم اضافه شدن.

round16_prediction_form_updated_8games.pdf


khob dadam in aro javab dad









marhale 2 dobare bego 

koja bezanam


yebar ke ono dorst kardam 

alan khobe ?


ino dad


Pasted text.txt
Document
ino dad


in?

ino dad


khobe



injoriye






kojast


alan value 2vomi chi bezanam



alan user password chejori mizare

alan yeki az dostam test karde emtiazesh hesab nemishe 



Pasted text(1).txt
Document
ino dade

injori shod bahal nist hamon ghabli behtare

Thought for 2m 36s
آماده‌ست؛ فرم PDF پیش‌بینی مرحله حذفی / Quarter Final رو ساختم:

knockout_prediction_form_qf.pdf


Stopped thinking

Thought for 2m 15s
درست شد؛ Colombia رو عوض کردم به Switzerland.

knockout_prediction_form_qf_swiss.pdf


i want to see player name  here is it possible 

Sunday 7:00 PM
tebghe in dorost kon dg 


baziaro chera neshon nemide






inam jadide










inam dashte bash ghanone emtiazaram ke midoni man 3 ta form farid 1 farid 2 farid 3 ro daram   mikham bebina behtarin pishbini man chi mitone bashe to formhaye nmokhtalefam chi bezanam behtare

rules ine mikham bebinam behtarin semario bara pishbini chiye dg

in axe 

nega kon in vaziyte alane jadvale emtiaza hast 

inam  pishbini in bacheha az gharemane  male khodamam hast farid 1  2 3 

inam vaziyat emtiaz dehiye va rules hast 

hala man 3 ta form daram va mikham komak koni ke behtarin vaziyat baram pish biad emroz bayd ino sabt konam






حتماً، طبق همان ترتیب جدول Players که در Supabase داری:

Name	Bonus
Admin User	0
farid1	7
kasra1	10
kasra2	0
kasra3	0
kian1	0
kian2	5
karim	17
mehdi	5
sinaz1	10
mahyarv1	10
mahyarv2	0
sinaz2	0
shahriyar	7
mahmud1	7
mahmud2	0
alish1	5
alish2	10
arsalan	0
raein	0
tofigh	10
shayan	0
mosafa1	10
mosafa2	0
poyan	0
ahangari	0
st1	7
st2	0
alik1	0
alik2	0
amir	0
farid2	0
farid3	10
بعد از اینکه این‌ها را وارد کردی، مرحله بعد فقط تغییر کد Leaderboard است تا از:

previous_points
به:

previous_points + COALESCE(Bonus, 0)
یا معادلش در React تغییر کند. این کار باعث می‌شود هر وقت Bonus را تغییر دادی، جدول به‌صورت خودکار امتیاز جدید را نشان بدهد.


Kkoja yani

منظورت از «کجا» اینه که این عددها را کجا وارد کنی:

داخل همان جدول players در Supabase، در ستون Bonus.

یعنی برای هر ردیف، روبه‌روی اسم همان شخص، داخل سلول ستون Bonus عددش را بنویس.

مثلاً:

ردیف farid1 → داخل ستون Bonus بزن 7

ردیف kasra1 → بزن 10

ردیف karim → بزن 17

ردیف farid3 → بزن 10

کسانی که امتیاز ندارند → بزن 0

بعد از واردکردن هر چند عدد، بالای جدول روی دکمه سبز Save یا Update بزن تا ذخیره شود.

فعلاً فقط همین کار را انجام بده؛ هنوز سراغ کد Leaderboard نرو.


Na taghire code leader board koja bedam

داخل فایل مربوط به صفحه‌ی Leaderboard در پروژه‌ات.

به احتمال زیاد مسیرش یکی از این‌هاست:

src/pages/Leaderboard.jsx
یا:

src/pages/Leaderboard.tsx
یا شاید:

src/components/Leaderboard.jsx
الان این کار را بکن
پروژه را در VS Code باز کن.

از سمت چپ پوشه src را باز کن.

داخلش دنبال فایلی بگرد که اسمش Leaderboard است.

فایل را باز کن.

داخل فایل با Ctrl + F این عبارت‌ها را یکی‌یکی جست‌وجو کن:

previous_points
یا:

totalPoints
یا:

points
احتمالاً جایی شبیه این می‌بینی:

const totalPoints = player.previous_points;
یا:

score: player.previous_points
باید به این تغییرش بدهی:

const totalPoints =
  (player.previous_points ?? 0) + (player.Bonus ?? 0);
نکته مهم: چون اسم ستونت در Supabase را Bonus با B بزرگ گذاشتی، فعلاً در کد هم باید همین را بنویسی:

player.Bonus
نه:

player.bonus
اما قبل از تغییر، یک عکس از فایل Leaderboard یا کدش بفرست تا دقیقاً همان خط درست را بهت بگویم و چیزی را اشتباه عوض نکنی.


prompt osh midi bedam cluad ke fght copy paste conam havaset bashe esme file bonus zadim

حتماً. این پرامپت را کامل به Claude بده. فقط کپی/پیست کن:

I need you to update my existing React + Vite + Supabase World Cup Prediction project.

IMPORTANT:
Do NOT redesign or refactor anything.
Do NOT change the UI.
Do NOT change the database structure except what is described below.
Only make the minimum required changes.

CURRENT DATABASE

Table:
players

Current columns include:

- previous_points
- Bonus

IMPORTANT:
The bonus column is named exactly:

Bonus

with a capital "B".

Do NOT rename it.

GOAL

I have manually entered bonus points into the players table.

Bonus contains the original prediction bonus:

- Third place
- Fourth place
- Golden Boot
- Later Champion
- Later Runner-up

Now I want the Leaderboard to automatically display:

Total Points =
previous_points + Bonus

where Bonus may be NULL.

So the calculation must safely use:

(previous_points ?? 0) + (Bonus ?? 0)

or the SQL equivalent.

TASKS

1.
Find the file responsible for rendering the leaderboard.

It may be named something like:

Leaderboard.tsx
Leaderboard.jsx
LeaderboardPage.tsx
etc.

2.
Find where the current score is displayed.

It is currently using previous_points only.

Replace it so it displays:

previous_points + Bonus

3.
Handle NULL values safely.

Bonus NULL must be treated as 0.

4.
Do NOT change sorting behaviour except that sorting must now use the new calculated total.

The leaderboard should rank players using:

previous_points + Bonus

instead of previous_points alone.

5.
Do NOT modify any other logic.

Do NOT change authentication.
Do NOT change prediction scoring.
Do NOT change admin pages.

OUTPUT

Show me exactly:

- which file you changed
- every line you changed
- explain why

Do not rewrite unrelated code.

Return the modified code ready for copy/paste.
این پرامپت دقیقاً چیزی را که می‌خواهی انجام می‌دهد و چون اسم ستون را Bonus گذاشتی، به Claude هم تأکید می‌کند که از Bonus با B بزرگ استفاده کند و آن را به bonus تغییر ندهد.



Pasted code.js
JavaScript
ino dorost kon bede man

درستش کردم. حالا ستون Bonus با B بزرگ از Supabase خوانده می‌شود، داخل امتیاز کل حساب می‌شود، رتبه‌بندی بر اساس امتیاز جدید مرتب می‌شود و یک ستون Bonus هم در جدول نمایش داده می‌شود. 


LeaderboardPage_fixed.jsx

فایل فعلی Leaderboard پروژه‌ات را با این فایل جایگزین کن. فرمول جدید:

Total Points =
 ​:contentReference[oaicite:1]{index=1}​
Minified React error #185; visit https://react.dev/errors/185 for the full message or use the non-minified dev environment for full errors and additional helpful warnings.


Retry

Library
/
LeaderboardPage_fixed.jsx


import { useState, useEffect } from 'react';
import { supabase } from '../lib/supabase';

const getTeamFlag = (teamName) => {
  const flags = {
    France: '🇫🇷',
    Morocco: '🇲🇦',
    England: '🏴󠁧󠁢󠁥󠁮󠁧󠁿',
    Norway: '🇳🇴',
    Belgium: '🇧🇪',
    Spain: '🇪🇸',
    Argentina: '🇦🇷',
    Colombia: '🇨🇴'
  };
  return flags[teamName] || '⚽';
};

const getMethodLabel = (method) => {
  if (method === '90') return '90';
  if (method === '120') return '120';
  if (method === 'Pen') return 'Pen';
  return method || '';
};

export default function LeaderboardPage() {
  const [leaderboard, setLeaderboard] = useState([]);
  const [qfMatches, setQfMatches] = useState([]);
  const [sfMatches, setSfMatches] = useState([]);
  const [finalMatches, setFinalMatches] = useState([]);
  const [qfPredictionsByPlayer, setQfPredictionsByPlayer] = useState({});
  const [sfPredictionsByPlayer, setSfPredictionsByPlayer] = useState({});
  const [finalPredictionsByPlayer, setFinalPredictionsByPlayer] = useState({});
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  const [now, setNow] = useState(new Date());

  useEffect(() => {
    fetchLeaderboard();
    const fetchInterval = setInterval(() => {
      fetchLeaderboard(false);
    }, 10000);
    const clockInterval = setInterval(() => {
      setNow(new Date());
    }, 30000);
    return () => {
      clearInterval(fetchInterval);
      clearInterval(clockInterval);
    };
  }, []);

  const fetchLeaderboard = async (showLoading = true) => {
    try {
      if (showLoading) setLoading(true);
      setError('');

      const { data: playersData, error: playersError } = await supabase
        .from('players')
        .select('*');

      if (playersError) throw playersError;

      const { data: qfMatchesData, error: qfMatchesError } = await supabase
        .from('qf_matches')
        .select('*')
        .order('match_number', { ascending: true });

      if (qfMatchesError) throw qfMatchesError;

      const { data: qfPredictionsData, error: qfPredictionsError } = await supabase
        .from('qf_predictions')
        .select('*');

      if (qfPredictionsError) throw qfPredictionsError;

      const { data: sfMatchesData, error: sfMatchesError } = await supabase
        .from('sf_matches')
        .select('*')
        .order('match_number', { ascending: true });

      if (sfMatchesError) throw sfMatchesError;

      const { data: sfPredictionsData, error: sfPredictionsError } = await supabase
        .from('sf_predictions')
        .select('*');

      if (sfPredictionsError) throw sfPredictionsError;

      const { data: finalMatchesData, error: finalMatchesError } = await supabase
        .from('final_matches')
        .select('*')
        .order('match_number', { ascending: true });

      if (finalMatchesError) throw finalMatchesError;

      const { data: finalPredictionsData, error: finalPredictionsError } = await supabase
        .from('final_predictions')
        .select('*');

      if (finalPredictionsError) throw finalPredictionsError;

      const players = playersData || [];
      const allQfMatches = qfMatchesData || [];
      const qfPredictions = qfPredictionsData || [];
      const allSfMatches = sfMatchesData || [];
      const sfPredictions = sfPredictionsData || [];
      const allFinalMatches = finalMatchesData || [];
      const finalPredictions = finalPredictionsData || [];

      const nonAdminPlayers = players.filter((p) => !p.is_admin);

      const qfPredMap = {};
      qfPredictions.forEach((pred) => {
        if (!qfPredMap[pred.player_id]) {
          qfPredMap[pred.player_id] = {};
        }
        qfPredMap[pred.player_id][pred.match_id] = pred;
      });

      const sfPredMap = {};
      sfPredictions.forEach((pred) => {
        if (!sfPredMap[pred.player_id]) {
          sfPredMap[pred.player_id] = {};
        }
        sfPredMap[pred.player_id][pred.match_id] = pred;
      });

      const finalPredMap = {};
      finalPredictions.forEach((pred) => {
        if (!finalPredMap[pred.player_id]) {
          finalPredMap[pred.player_id] = {};
        }
        finalPredMap[pred.player_id][pred.match_id] = pred;
      });

      const rows = nonAdminPlayers.map((player) => {
        const playerQfPredictions = qfPredictions.filter(
          (pred) => pred.player_id === player.id
        );
        const playerSfPredictions = sfPredictions.filter(
          (pred) => pred.player_id === player.id
        );
        const playerFinalPredictions = finalPredictions.filter(
          (pred) => pred.player_id === player.id
        );

        const qfPoints = playerQfPredictions.reduce(
          (sum, pred) => sum + (pred.points || 0),
          0
        );
        const sfPoints = playerSfPredictions.reduce(
          (sum, pred) => sum + (pred.points || 0),
          0
        );
        const finalStagePoints = playerFinalPredictions.reduce(
          (sum, pred) => sum + (pred.points || 0),
          0
        );

        const previousPoints = player.previous_points ?? 0;
        const bonusPoints = player.Bonus ?? 0;
        const totalPoints =
          previousPoints + bonusPoints + qfPoints + sfPoints + finalStagePoints;

        return {
          playerId: player.id,
          name: player.name,
          previousPoints,
          bonusPoints,
          qfPoints,
          sfPoints,
          finalStagePoints,
          totalPoints,
          predictionsSubmitted:
            playerQfPredictions.length +
            playerSfPredictions.length +
            playerFinalPredictions.length
        };
      });

      rows.sort((a, b) => {
        if (b.totalPoints !== a.totalPoints) {
          return b.totalPoints - a.totalPoints;
        }
        return a.name.localeCompare(b.name);
      });

      let currentRank = 0;
      let previousTotal = null;
      const rankedRows = rows.map((row, index) => {
        if (row.totalPoints !== previousTotal) {
          currentRank = index + 1;
          previousTotal = row.totalPoints;
        }
        return { ...row, rank: currentRank };
      });

      setLeaderboard(rankedRows);
      setQfMatches(allQfMatches);
      setSfMatches(allSfMatches);
      setFinalMatches(allFinalMatches);
      setQfPredictionsByPlayer(qfPredMap);
      setSfPredictionsByPlayer(sfPredMap);
      setFinalPredictionsByPlayer(finalPredMap);
    } catch (err) {
      setError(`Failed to load leaderboard: ${err.message}`);
    } finally {
      if (showLoading) setLoading(false);
    }
  };

  const isQfMatchRevealed = (match) => {
    const revealTime = new Date(new Date(match.kickoff_time).getTime() - 60 * 60 * 1000);
    return now >= revealTime;
  };

  const getSfRevealTime = () => {
    if (!sfMatches || sfMatches.length === 0) return null;
    const firstMatch = [...sfMatches].sort(
      (a, b) => new Date(a.kickoff_time) - new Date(b.kickoff_time)
    )[0];
    return new Date(new Date(firstMatch.kickoff_time).getTime() - 60 * 60 * 1000);
  };

  const isSfRevealed = () => {
    const revealTime = getSfRevealTime();
    if (!revealTime) return false;
    return now >= revealTime;
  };

  const getFinalRevealTime = () => {
    if (!finalMatches || finalMatches.length === 0) return null;
    const firstMatch = [...finalMatches].sort(
      (a, b) => new Date(a.kickoff_time) - new Date(b.kickoff_time)
    )[0];
    return new Date(new Date(firstMatch.kickoff_time).getTime() - 60 * 60 * 1000);
  };

  const isFinalRevealed = () => {
    const revealTime = getFinalRevealTime();
    if (!revealTime) return false;
    return now >= revealTime;
  };

  const revealedQfMatches = qfMatches.filter(isQfMatchRevealed);
  const revealedSfMatches = isSfRevealed() ? sfMatches : [];
  const revealedFinalMatches = isFinalRevealed() ? finalMatches : [];

  const getFinalHeaderPrefix = (match) => {
    if (match.round_type === 'Final') return 'Final';
    return '3rd';
  };

  const getRankDisplay = (rank) => {
    if (rank === 1) return '🥇';
    if (rank === 2) return '🥈';
    if (rank === 3) return '🥉';
    return rank;
  };

  const renderPredictionCell = (predMap, playerId, match) => {
    const pred = predMap[playerId]?.[match.id];

    if (
      !pred ||
      pred.predicted_team_a_score === null ||
      pred.predicted_team_a_score === undefined
    ) {
      return <span className="text-slate-600">—</span>;
    }

    return (
      <div className="text-xs leading-relaxed">
        <p className="font-bold text-white">
          {pred.predicted_team_a_score} - {pred.predicted_team_b_score}
        </p>
        <p className="text-slate-400">
          {getTeamFlag(pred.predicted_qualified_team)} {pred.predicted_qualified_team}
        </p>
        <p className="text-slate-500">{getMethodLabel(pred.predicted_winning_method)}</p>
      </div>
    );
  };

  const totalPlayers = leaderboard.length;
  const totalPredictions = leaderboard.reduce(
    (sum, row) => sum + row.predictionsSubmitted,
    0
  );
  const highestScore = leaderboard.length > 0 ? leaderboard[0].totalPoints : 0;

  if (loading) {
    return (
      <div className="min-h-screen bg-gradient-to-b from-slate-950 via-slate-900 to-slate-950 p-8">
        <div className="max-w-6xl mx-auto">
          <div className="flex items-center justify-center py-20">
            <div className="text-center">
              <div className="inline-block animate-spin rounded-full h-12 w-12 border-b-2 border-emerald-500 mb-4"></div>
              <p className="text-slate-400">Loading leaderboard...</p>
            </div>
          </div>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-gradient-to-b from-slate-950 via-slate-900 to-slate-950 p-4 md:p-8">
      <div className="max-w-6xl mx-auto">
        <div className="flex flex-col sm:flex-row sm:justify-between sm:items-end gap-4 mb-8">
          <div>
            <p className="text-emerald-400 text-xs font-semibold tracking-widest uppercase mb-2">
              World Cup Prediction League
            </p>
            <h1 className="text-3xl md:text-4xl font-bold text-white">
              🏆 Leaderboard
            </h1>
            <p className="text-slate-400 mt-2">
              Who is leading the league? Updates automatically every 10 seconds.
            </p>
          </div>
          <button
            onClick={() => fetchLeaderboard(true)}
            className="self-start sm:self-auto px-5 py-2.5 rounded-xl text-sm font-semibold text-white bg-slate-800 border border-slate-700 hover:bg-slate-700 hover:border-slate-600 active:bg-slate-600 transition-colors"
          >
            ↻ Refresh
          </button>
        </div>

        {error && (
          <div className="mb-6 p-4 rounded-xl border bg-red-950/50 text-red-300 border-red-900">
            <p className="text-sm font-medium">{error}</p>
          </div>
        )}

        <div className="grid grid-cols-3 gap-3 md:gap-5 mb-8">
          <div className="bg-slate-900 border border-slate-800 rounded-2xl p-4 md:p-6 text-center">
            <p className="text-2xl md:text-3xl font-bold text-white">{totalPlayers}</p>
            <p className="text-xs md:text-sm text-slate-400 mt-1">Total Players</p>
          </div>
          <div className="bg-slate-900 border border-slate-800 rounded-2xl p-4 md:p-6 text-center">
            <p className="text-2xl md:text-3xl font-bold text-white">{totalPredictions}</p>
            <p className="text-xs md:text-sm text-slate-400 mt-1">Total Predictions</p>
          </div>
          <div className="bg-slate-900 border border-emerald-900/60 rounded-2xl p-4 md:p-6 text-center">
            <p className="text-2xl md:text-3xl font-bold text-emerald-400">{highestScore}</p>
            <p className="text-xs md:text-sm text-slate-400 mt-1">Highest Score</p>
          </div>
        </div>

        {revealedQfMatches.length === 0 &&
          revealedSfMatches.length === 0 &&
          revealedFinalMatches.length === 0 && (
            <div className="mb-6 p-4 bg-slate-900/60 border border-slate-800 rounded-2xl">
              <p className="text-sm text-slate-400 text-center">
                🔒 QF predictions are revealed 1 hour before each match. SF and Final stage
                predictions are revealed 1 hour before the first match of each stage.
              </p>
            </div>
          )}

        {leaderboard.length === 0 && !error ? (
          <div className="bg-slate-900 border border-slate-800 rounded-2xl p-10 text-center">
            <p className="text-slate-400">No players found.</p>
          </div>
        ) : (
          <div className="bg-slate-900 border border-slate-800 rounded-2xl overflow-hidden">
            <div className="overflow-x-auto">
              <table className="w-full">
                <thead>
                  <tr className="border-b border-slate-800 bg-slate-900/80">
                    <th className="px-3 md:px-5 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      Rank
                    </th>
                    <th className="px-3 md:px-5 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      Player
                    </th>
                    <th className="px-3 md:px-5 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      Previous Points
                    </th>
                    <th className="px-3 md:px-5 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      Bonus
                    </th>
                    <th className="px-3 md:px-5 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      QF Points
                    </th>
                    <th className="px-3 md:px-5 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      SF Points
                    </th>
                    <th className="px-3 md:px-5 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      Final Stage Points
                    </th>
                    <th className="px-3 md:px-5 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      Total Points
                    </th>
                    <th className="px-3 md:px-5 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      Predictions
                    </th>
                    {revealedQfMatches.map((match) => (
                      <th
                        key={`qf_${match.id}`}
                        className="px-3 md:px-5 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap"
                      >
                        QF{match.match_number}: {getTeamFlag(match.team_a)} {match.team_a}
                        {' - '}
                        {getTeamFlag(match.team_b)} {match.team_b}
                      </th>
                    ))}
                    {revealedSfMatches.map((match) => (
                      <th
                        key={`sf_${match.id}`}
                        className="px-3 md:px-5 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap"
                      >
                        SF{match.match_number}: {getTeamFlag(match.team_a)} {match.team_a}
                        {' - '}
                        {getTeamFlag(match.team_b)} {match.team_b}
                      </th>
                    ))}
                    {revealedFinalMatches.map((match) => (
                      <th
                        key={`final_${match.id}`}
                        className="px-3 md:px-5 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap"
                      >
                        {getFinalHeaderPrefix(match)}: {getTeamFlag(match.team_a)}{' '}
                        {match.team_a}
                        {' - '}
                        {getTeamFlag(match.team_b)} {match.team_b}
                      </th>
                    ))}
                  </tr>
                </thead>
                <tbody className="divide-y divide-slate-800">
                  {leaderboard.map((row) => {
                    const isFirst = row.rank === 1;
                    const isTopThree = row.rank <= 3;

                    return (
                      <tr
                        key={row.playerId}
                        className={`transition-colors ${
                          isFirst
                            ? 'bg-emerald-950/40 hover:bg-emerald-950/60'
                            : 'hover:bg-slate-800/50'
                        }`}
                      >
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap">
                          <span
                            className={`inline-flex items-center justify-center h-9 w-9 rounded-full font-bold ${
                              isTopThree
                                ? 'text-xl'
                                : 'text-sm bg-slate-800 text-slate-400'
                            }`}
                          >
                            {getRankDisplay(row.rank)}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap">
                          <span
                            className={`text-sm font-semibold ${
                              isFirst ? 'text-emerald-300' : 'text-white'
                            }`}
                          >
                            {row.name}
                            {isFirst && (
                              <span className="ml-2 text-xs font-bold text-emerald-400 bg-emerald-950 border border-emerald-900 px-2 py-0.5 rounded-full align-middle">
                                LEADER
                              </span>
                            )}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap text-right">
                          <span className="text-sm text-slate-400">
                            {row.previousPoints}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap text-right">
                          <span className="text-sm font-semibold text-amber-300">
                            {row.bonusPoints}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap text-right">
                          <span className="text-sm font-semibold text-white">
                            {row.qfPoints}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap text-right">
                          <span className="text-sm font-semibold text-white">
                            {row.sfPoints}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap text-right">
                          <span className="text-sm font-semibold text-white">
                            {row.finalStagePoints}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap text-right">
                          <span
                            className={`text-base font-bold ${
                              isFirst ? 'text-emerald-400' : 'text-white'
                            }`}
                          >
                            {row.totalPoints}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap text-right">
                          <span className="text-sm text-slate-400">
                            {row.predictionsSubmitted} / 8
                          </span>
                        </td>
                        {revealedQfMatches.map((match) => (
                          <td
                            key={`qf_${match.id}`}
                            className="px-3 md:px-5 py-4 whitespace-nowrap"
                          >
                            {renderPredictionCell(qfPredictionsByPlayer, row.playerId, match)}
                          </td>
                        ))}
                        {revealedSfMatches.map((match) => (
                          <td
                            key={`sf_${match.id}`}
                            className="px-3 md:px-5 py-4 whitespace-nowrap"
                          >
                            {renderPredictionCell(sfPredictionsByPlayer, row.playerId, match)}
                          </td>
                        ))}
                        {revealedFinalMatches.map((match) => (
                          <td
                            key={`final_${match.id}`}
                            className="px-3 md:px-5 py-4 whitespace-nowrap"
                          >
                            {renderPredictionCell(
                              finalPredictionsByPlayer,
                              row.playerId,
                              match
                            )}
                          </td>
                        ))}
                      </tr>
                    );
                  })}
                </tbody>
              </table>
            </div>
          </div>
        )}

        <div className="mt-6 p-4 bg-slate-900/60 border border-slate-800 rounded-2xl">
          <p className="text-sm text-slate-400 text-center">
            <span className="text-emerald-400 font-semibold">Total Points</span> = Previous +
            Bonus + QF + SF + Final Stage ·{' '}
            <span className="text-emerald-400 font-semibold">QF:</span> Qualify +5 · Score +2 ·
            Method +1 · Max 8 ·{' '}
            <span className="text-blue-400 font-semibold">SF:</span> Qualify +7 · Score +3 ·
            Method +1 · Max 11 ·{' '}
            <span className="text-yellow-400 font-semibold">Final:</span> Champion +10 · Score
            +5 · Method +1 · Max 16 ·{' '}
            <span className="text-yellow-400 font-semibold">3rd Place:</span> Winner +7 · Score
            +3 · Method +1 · Max 11
          </p>
        </div>
      </div>
    </div>
  );
}
Library
/
LeaderboardPage_fixed.jsx


import { useState, useEffect } from 'react';
import { supabase } from '../lib/supabase';

const getTeamFlag = (teamName) => {
  const flags = {
    France: '🇫🇷',
    Morocco: '🇲🇦',
    England: '🏴󠁧󠁢󠁥󠁮󠁧󠁿',
    Norway: '🇳🇴',
    Belgium: '🇧🇪',
    Spain: '🇪🇸',
    Argentina: '🇦🇷',
    Colombia: '🇨🇴'
  };
  return flags[teamName] || '⚽';
};

const getMethodLabel = (method) => {
  if (method === '90') return '90';
  if (method === '120') return '120';
  if (method === 'Pen') return 'Pen';
  return method || '';
};

export default function LeaderboardPage() {
  const [leaderboard, setLeaderboard] = useState([]);
  const [qfMatches, setQfMatches] = useState([]);
  const [sfMatches, setSfMatches] = useState([]);
  const [finalMatches, setFinalMatches] = useState([]);
  const [qfPredictionsByPlayer, setQfPredictionsByPlayer] = useState({});
  const [sfPredictionsByPlayer, setSfPredictionsByPlayer] = useState({});
  const [finalPredictionsByPlayer, setFinalPredictionsByPlayer] = useState({});
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  const [now, setNow] = useState(new Date());

  useEffect(() => {
    fetchLeaderboard();
    const fetchInterval = setInterval(() => {
      fetchLeaderboard(false);
    }, 10000);
    const clockInterval = setInterval(() => {
      setNow(new Date());
    }, 30000);
    return () => {
      clearInterval(fetchInterval);
      clearInterval(clockInterval);
    };
  }, []);

  const fetchLeaderboard = async (showLoading = true) => {
    try {
      if (showLoading) setLoading(true);
      setError('');

      const { data: playersData, error: playersError } = await supabase
        .from('players')
        .select('*');

      if (playersError) throw playersError;

      const { data: qfMatchesData, error: qfMatchesError } = await supabase
        .from('qf_matches')
        .select('*')
        .order('match_number', { ascending: true });

      if (qfMatchesError) throw qfMatchesError;

      const { data: qfPredictionsData, error: qfPredictionsError } = await supabase
        .from('qf_predictions')
        .select('*');

      if (qfPredictionsError) throw qfPredictionsError;

      const { data: sfMatchesData, error: sfMatchesError } = await supabase
        .from('sf_matches')
        .select('*')
        .order('match_number', { ascending: true });

      if (sfMatchesError) throw sfMatchesError;

      const { data: sfPredictionsData, error: sfPredictionsError } = await supabase
        .from('sf_predictions')
        .select('*');

      if (sfPredictionsError) throw sfPredictionsError;

      const { data: finalMatchesData, error: finalMatchesError } = await supabase
        .from('final_matches')
        .select('*')
        .order('match_number', { ascending: true });

      if (finalMatchesError) throw finalMatchesError;

      const { data: finalPredictionsData, error: finalPredictionsError } = await supabase
        .from('final_predictions')
        .select('*');

      if (finalPredictionsError) throw finalPredictionsError;

      const players = playersData || [];
      const allQfMatches = qfMatchesData || [];
      const qfPredictions = qfPredictionsData || [];
      const allSfMatches = sfMatchesData || [];
      const sfPredictions = sfPredictionsData || [];
      const allFinalMatches = finalMatchesData || [];
      const finalPredictions = finalPredictionsData || [];

      const nonAdminPlayers = players.filter((p) => !p.is_admin);

      const qfPredMap = {};
      qfPredictions.forEach((pred) => {
        if (!qfPredMap[pred.player_id]) {
          qfPredMap[pred.player_id] = {};
        }
        qfPredMap[pred.player_id][pred.match_id] = pred;
      });

      const sfPredMap = {};
      sfPredictions.forEach((pred) => {
        if (!sfPredMap[pred.player_id]) {
          sfPredMap[pred.player_id] = {};
        }
        sfPredMap[pred.player_id][pred.match_id] = pred;
      });

      const finalPredMap = {};
      finalPredictions.forEach((pred) => {
        if (!finalPredMap[pred.player_id]) {
          finalPredMap[pred.player_id] = {};
        }
        finalPredMap[pred.player_id][pred.match_id] = pred;
      });

      const rows = nonAdminPlayers.map((player) => {
        const playerQfPredictions = qfPredictions.filter(
          (pred) => pred.player_id === player.id
        );
        const playerSfPredictions = sfPredictions.filter(
          (pred) => pred.player_id === player.id
        );
        const playerFinalPredictions = finalPredictions.filter(
          (pred) => pred.player_id === player.id
        );

        const qfPoints = playerQfPredictions.reduce(
          (sum, pred) => sum + (pred.points || 0),
          0
        );
        const sfPoints = playerSfPredictions.reduce(
          (sum, pred) => sum + (pred.points || 0),
          0
        );
        const finalStagePoints = playerFinalPredictions.reduce(
          (sum, pred) => sum + (pred.points || 0),
          0
        );

        const previousPoints = player.previous_points ?? 0;
        const bonusPoints = player.Bonus ?? 0;
        const totalPoints =
          previousPoints + bonusPoints + qfPoints + sfPoints + finalStagePoints;

        return {
          playerId: player.id,
          name: player.name,
          previousPoints,
          bonusPoints,
          qfPoints,
          sfPoints,
          finalStagePoints,
          totalPoints,
          predictionsSubmitted:
            playerQfPredictions.length +
            playerSfPredictions.length +
            playerFinalPredictions.length
        };
      });

      rows.sort((a, b) => {
        if (b.totalPoints !== a.totalPoints) {
          return b.totalPoints - a.totalPoints;
        }
        return a.name.localeCompare(b.name);
      });

      let currentRank = 0;
      let previousTotal = null;
      const rankedRows = rows.map((row, index) => {
        if (row.totalPoints !== previousTotal) {
          currentRank = index + 1;
          previousTotal = row.totalPoints;
        }
        return { ...row, rank: currentRank };
      });

      setLeaderboard(rankedRows);
      setQfMatches(allQfMatches);
      setSfMatches(allSfMatches);
      setFinalMatches(allFinalMatches);
      setQfPredictionsByPlayer(qfPredMap);
      setSfPredictionsByPlayer(sfPredMap);
      setFinalPredictionsByPlayer(finalPredMap);
    } catch (err) {
      setError(`Failed to load leaderboard: ${err.message}`);
    } finally {
      if (showLoading) setLoading(false);
    }
  };

  const isQfMatchRevealed = (match) => {
    const revealTime = new Date(new Date(match.kickoff_time).getTime() - 60 * 60 * 1000);
    return now >= revealTime;
  };

  const getSfRevealTime = () => {
    if (!sfMatches || sfMatches.length === 0) return null;
    const firstMatch = [...sfMatches].sort(
      (a, b) => new Date(a.kickoff_time) - new Date(b.kickoff_time)
    )[0];
    return new Date(new Date(firstMatch.kickoff_time).getTime() - 60 * 60 * 1000);
  };

  const isSfRevealed = () => {
    const revealTime = getSfRevealTime();
    if (!revealTime) return false;
    return now >= revealTime;
  };

  const getFinalRevealTime = () => {
    if (!finalMatches || finalMatches.length === 0) return null;
    const firstMatch = [...finalMatches].sort(
      (a, b) => new Date(a.kickoff_time) - new Date(b.kickoff_time)
    )[0];
    return new Date(new Date(firstMatch.kickoff_time).getTime() - 60 * 60 * 1000);
  };

  const isFinalRevealed = () => {
    const revealTime = getFinalRevealTime();
    if (!revealTime) return false;
    return now >= revealTime;
  };

  const revealedQfMatches = qfMatches.filter(isQfMatchRevealed);
  const revealedSfMatches = isSfRevealed() ? sfMatches : [];
  const revealedFinalMatches = isFinalRevealed() ? finalMatches : [];

  const getFinalHeaderPrefix = (match) => {
    if (match.round_type === 'Final') return 'Final';
    return '3rd';
  };

  const getRankDisplay = (rank) => {
    if (rank === 1) return '🥇';
    if (rank === 2) return '🥈';
    if (rank === 3) return '🥉';
    return rank;
  };

  const renderPredictionCell = (predMap, playerId, match) => {
    const pred = predMap[playerId]?.[match.id];

    if (
      !pred ||
      pred.predicted_team_a_score === null ||
      pred.predicted_team_a_score === undefined
    ) {
      return <span className="text-slate-600">—</span>;
    }

    return (
      <div className="text-xs leading-relaxed">
        <p className="font-bold text-white">
          {pred.predicted_team_a_score} - {pred.predicted_team_b_score}
        </p>
        <p className="text-slate-400">
          {getTeamFlag(pred.predicted_qualified_team)} {pred.predicted_qualified_team}
        </p>
        <p className="text-slate-500">{getMethodLabel(pred.predicted_winning_method)}</p>
      </div>
    );
  };

  const totalPlayers = leaderboard.length;
  const totalPredictions = leaderboard.reduce(
    (sum, row) => sum + row.predictionsSubmitted,
    0
  );
  const highestScore = leaderboard.length > 0 ? leaderboard[0].totalPoints : 0;

  if (loading) {
    return (
      <div className="min-h-screen bg-gradient-to-b from-slate-950 via-slate-900 to-slate-950 p-8">
        <div className="max-w-6xl mx-auto">
          <div className="flex items-center justify-center py-20">
            <div className="text-center">
              <div className="inline-block animate-spin rounded-full h-12 w-12 border-b-2 border-emerald-500 mb-4"></div>
              <p className="text-slate-400">Loading leaderboard...</p>
            </div>
          </div>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-gradient-to-b from-slate-950 via-slate-900 to-slate-950 p-4 md:p-8">
      <div className="max-w-6xl mx-auto">
        <div className="flex flex-col sm:flex-row sm:justify-between sm:items-end gap-4 mb-8">
          <div>
            <p className="text-emerald-400 text-xs font-semibold tracking-widest uppercase mb-2">
              World Cup Prediction League
            </p>
            <h1 className="text-3xl md:text-4xl font-bold text-white">
              🏆 Leaderboard
            </h1>
            <p className="text-slate-400 mt-2">
              Who is leading the league? Updates automatically every 10 seconds.
            </p>
          </div>
          <button
            onClick={() => fetchLeaderboard(true)}
            className="self-start sm:self-auto px-5 py-2.5 rounded-xl text-sm font-semibold text-white bg-slate-800 border border-slate-700 hover:bg-slate-700 hover:border-slate-600 active:bg-slate-600 transition-colors"
          >
            ↻ Refresh
          </button>
        </div>

        {error && (
          <div className="mb-6 p-4 rounded-xl border bg-red-950/50 text-red-300 border-red-900">
            <p className="text-sm font-medium">{error}</p>
          </div>
        )}

        <div className="grid grid-cols-3 gap-3 md:gap-5 mb-8">
          <div className="bg-slate-900 border border-slate-800 rounded-2xl p-4 md:p-6 text-center">
            <p className="text-2xl md:text-3xl font-bold text-white">{totalPlayers}</p>
            <p className="text-xs md:text-sm text-slate-400 mt-1">Total Players</p>
          </div>
          <div className="bg-slate-900 border border-slate-800 rounded-2xl p-4 md:p-6 text-center">
            <p className="text-2xl md:text-3xl font-bold text-white">{totalPredictions}</p>
            <p className="text-xs md:text-sm text-slate-400 mt-1">Total Predictions</p>
          </div>
          <div className="bg-slate-900 border border-emerald-900/60 rounded-2xl p-4 md:p-6 text-center">
            <p className="text-2xl md:text-3xl font-bold text-emerald-400">{highestScore}</p>
            <p className="text-xs md:text-sm text-slate-400 mt-1">Highest Score</p>
          </div>
        </div>

        {revealedQfMatches.length === 0 &&
          revealedSfMatches.length === 0 &&
          revealedFinalMatches.length === 0 && (
            <div className="mb-6 p-4 bg-slate-900/60 border border-slate-800 rounded-2xl">
              <p className="text-sm text-slate-400 text-center">
                🔒 QF predictions are revealed 1 hour before each match. SF and Final stage
                predictions are revealed 1 hour before the first match of each stage.
              </p>
            </div>
          )}

        {leaderboard.length === 0 && !error ? (
          <div className="bg-slate-900 border border-slate-800 rounded-2xl p-10 text-center">
            <p className="text-slate-400">No players found.</p>
          </div>
        ) : (
          <div className="bg-slate-900 border border-slate-800 rounded-2xl overflow-hidden">
            <div className="overflow-x-auto">
              <table className="w-full">
                <thead>
                  <tr className="border-b border-slate-800 bg-slate-900/80">
                    <th className="px-3 md:px-5 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      Rank
                    </th>
                    <th className="px-3 md:px-5 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      Player
                    </th>
                    <th className="px-3 md:px-5 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      Previous Points
                    </th>
                    <th className="px-3 md:px-5 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      Bonus
                    </th>
                    <th className="px-3 md:px-5 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      QF Points
                    </th>
                    <th className="px-3 md:px-5 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      SF Points
                    </th>
                    <th className="px-3 md:px-5 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      Final Stage Points
                    </th>
                    <th className="px-3 md:px-5 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      Total Points
                    </th>
                    <th className="px-3 md:px-5 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap">
                      Predictions
                    </th>
                    {revealedQfMatches.map((match) => (
                      <th
                        key={`qf_${match.id}`}
                        className="px-3 md:px-5 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap"
                      >
                        QF{match.match_number}: {getTeamFlag(match.team_a)} {match.team_a}
                        {' - '}
                        {getTeamFlag(match.team_b)} {match.team_b}
                      </th>
                    ))}
                    {revealedSfMatches.map((match) => (
                      <th
                        key={`sf_${match.id}`}
                        className="px-3 md:px-5 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap"
                      >
                        SF{match.match_number}: {getTeamFlag(match.team_a)} {match.team_a}
                        {' - '}
                        {getTeamFlag(match.team_b)} {match.team_b}
                      </th>
                    ))}
                    {revealedFinalMatches.map((match) => (
                      <th
                        key={`final_${match.id}`}
                        className="px-3 md:px-5 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider whitespace-nowrap"
                      >
                        {getFinalHeaderPrefix(match)}: {getTeamFlag(match.team_a)}{' '}
                        {match.team_a}
                        {' - '}
                        {getTeamFlag(match.team_b)} {match.team_b}
                      </th>
                    ))}
                  </tr>
                </thead>
                <tbody className="divide-y divide-slate-800">
                  {leaderboard.map((row) => {
                    const isFirst = row.rank === 1;
                    const isTopThree = row.rank <= 3;

                    return (
                      <tr
                        key={row.playerId}
                        className={`transition-colors ${
                          isFirst
                            ? 'bg-emerald-950/40 hover:bg-emerald-950/60'
                            : 'hover:bg-slate-800/50'
                        }`}
                      >
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap">
                          <span
                            className={`inline-flex items-center justify-center h-9 w-9 rounded-full font-bold ${
                              isTopThree
                                ? 'text-xl'
                                : 'text-sm bg-slate-800 text-slate-400'
                            }`}
                          >
                            {getRankDisplay(row.rank)}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap">
                          <span
                            className={`text-sm font-semibold ${
                              isFirst ? 'text-emerald-300' : 'text-white'
                            }`}
                          >
                            {row.name}
                            {isFirst && (
                              <span className="ml-2 text-xs font-bold text-emerald-400 bg-emerald-950 border border-emerald-900 px-2 py-0.5 rounded-full align-middle">
                                LEADER
                              </span>
                            )}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap text-right">
                          <span className="text-sm text-slate-400">
                            {row.previousPoints}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap text-right">
                          <span className="text-sm font-semibold text-amber-300">
                            {row.bonusPoints}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap text-right">
                          <span className="text-sm font-semibold text-white">
                            {row.qfPoints}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap text-right">
                          <span className="text-sm font-semibold text-white">
                            {row.sfPoints}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap text-right">
                          <span className="text-sm font-semibold text-white">
                            {row.finalStagePoints}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap text-right">
                          <span
                            className={`text-base font-bold ${
                              isFirst ? 'text-emerald-400' : 'text-white'
                            }`}
                          >
                            {row.totalPoints}
                          </span>
                        </td>
                        <td className="px-3 md:px-5 py-4 whitespace-nowrap text-right">
                          <span className="text-sm text-slate-400">
                            {row.predictionsSubmitted} / 8
                          </span>
                        </td>
                        {revealedQfMatches.map((match) => (
                          <td
                            key={`qf_${match.id}`}
                            className="px-3 md:px-5 py-4 whitespace-nowrap"
                          >
                            {renderPredictionCell(qfPredictionsByPlayer, row.playerId, match)}
                          </td>
                        ))}
                        {revealedSfMatches.map((match) => (
                          <td
                            key={`sf_${match.id}`}
                            className="px-3 md:px-5 py-4 whitespace-nowrap"
                          >
                            {renderPredictionCell(sfPredictionsByPlayer, row.playerId, match)}
                          </td>
                        ))}
                        {revealedFinalMatches.map((match) => (
                          <td
                            key={`final_${match.id}`}
                            className="px-3 md:px-5 py-4 whitespace-nowrap"
                          >
                            {renderPredictionCell(
                              finalPredictionsByPlayer,
                              row.playerId,
                              match
                            )}
                          </td>
                        ))}
                      </tr>
                    );
                  })}
                </tbody>
              </table>
            </div>
          </div>
        )}

        <div className="mt-6 p-4 bg-slate-900/60 border border-slate-800 rounded-2xl">
          <p className="text-sm text-slate-400 text-center">
            <span className="text-emerald-400 font-semibold">Total Points</span> = Previous +
            Bonus + QF + SF + Final Stage ·{' '}
            <span className="text-emerald-400 font-semibold">QF:</span> Qualify +5 · Score +2 ·
            Method +1 · Max 8 ·{' '}
            <span className="text-blue-400 font-semibold">SF:</span> Qualify +7 · Score +3 ·
            Method +1 · Max 11 ·{' '}
            <span className="text-yellow-400 font-semibold">Final:</span> Champion +10 · Score
            +5 · Method +1 · Max 16 ·{' '}
            <span className="text-yellow-400 font-semibold">3rd Place:</span> Winner +7 · Score
            +3 · Method +1 · Max 11
          </p>
        </div>
      </div>
    </div>
  );
}